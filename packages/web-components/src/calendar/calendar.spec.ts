import { spinalCase } from '@microsoft/fast-web-utilities';
import { expect, test } from '@playwright/test';
import type { Locator, Page } from '@playwright/test';
import { fixtureURL } from '../helpers.tests.js';

/**
 * TODO
 * mouse clicks on targeted elements
expected results (increment/decrement, etc.)
keydown on targeted elements
expected results (increment/decrement, aria attrs, etc.)
current date (today)
selected date
tabIndex on targeted element both before and after selection
focus of selected target both before and after selection

 */
